const coins = String.raw`    +
  ($) ($)
    ($)
  ($) ($)
  ------`;

const closedChest = String.raw`  .----.
 /____/|
 |--+-| |
 |____|/
  ------`;

const openChest = String.raw`  .----.
  | /\ |
 /$$$$/|
 |--+-| |
 |____|/`;

const mysteryItem = String.raw`    +
  .----.
  | ?  |
  '----'
    +`;

const gem = String.raw`   /\
  /\/\
  \  /
   \/
  ----`;

export default { coins, closedChest, openChest, mysteryItem, gem };
